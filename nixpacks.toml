[variables]
NODE_VERSION = "22.16"
NIXPACKS_NODE_VERSION = "22.16"

[phases.setup]
nixPkgs = ["nodejs_22", "corepack"]

[phases.install]
cmds = [
    "corepack enable",
    "corepack prepare pnpm@10.8 --activate",
    "pnpm install"
]

[phases.build]
cmds = ["pnpm run build"]